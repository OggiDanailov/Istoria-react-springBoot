spring.application.name=demo
server.port=8081

# ===== ACTIVE: PostgreSQL (for Docker) =====
spring.datasource.url=jdbc:postgresql://localhost:5432/ave_caesar_local
spring.datasource.username=oggi
spring.datasource.password=Zelda214!
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===== COMMENTED: H2 (local development) =====
# spring.datasource.url=jdbc:h2:file:./data/quizdb;DB_CLOSE_DELAY=-1
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# ===== Hibernate Settings =====
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===== Flyway (disabled while using Hibernate auto-create) =====
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
# spring.flyway.baselineOnMigrate=true
# spring.flyway.baselineVersion=1
# spring.flyway.baselineDescription=Initial schema with existing data

# ===== Environment Variables =====
spring.config.import=optional:file:.env[.properties]

# ===== JWT Configuration =====
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}